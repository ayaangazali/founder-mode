# FOUNDER MODE — FULL-REPO AUDIT (2026-07-13)
### Six parallel specialist reviews (sim core · render · features · tests · docs-vs-reality · server/deploy) + a conversation-history gap analysis.
### Read-only audit — nothing here changed game behavior. P0 = breaks a run / exploitable / governance-critical, P1 = visible flaw, P2 = smell/debt.
### Line numbers refer to index.html @ commit 1159a09 unless another file is named.

---

## EXECUTIVE SUMMARY

- **~70 distinct findings.** 4 P0s, ~14 P1s, the rest P2/debt.
- **The engine is healthy.** Fixed timestep, collision (bonk/one-way/taxi), stomp arming, checkpoint economics: all verified sound. The rot is at the edges: the *economy* (two money exploits that matter now that a leaderboard exists), *late-added content colliding with boss arenas*, and *state ticked inside draw()*.
- **The biggest single theme: the docs lie.** CLAUDE.md — the file every session reads first — misdescribes the game's size, zones, TODOs, and most critically still contains a "Never ship thin-veil celebs" rule that the shipped, deployed game (owner-approved, 3× briefed) directly violates. Until CLAUDE.md says the decided thing, every future session starts from a false constitution.
- **The test suite is deep on last month and blind to this week.** Cofounders, pedigree, mom round, interns, interview 2.0, CMO, pause, leaderboard client: zero committed assertions. And the three "canonical" gates (smoketest/playtest/deathtest) are log-only — they cannot fail short of a crash.
- **Two public endpoints need attention**: the leaderboard is forgeable by curl (no proof-of-run), and api/og.mjs is dead code that's still deployed, unauthenticated, and cache-busts into fresh renders per query.

---

## P0s (fix-first list)

| # | Where | Finding |
|---|---|---|
| P0-1 | index.html:1669 | **30 UNDER 30 is an infinite money printer**: a phantom spawns every 45s, each stomp pays +$15K, no cap — leaderboard scales with grind time |
| P0-2 | index.html:1719 | **Bell double-payout**: +$1M lands 110 frames before the win commits; dying during confetti keeps the money, respawns at 7240, and the bell re-arms for another +$1M (repeatable) |
| P0-3 | test/{smoketest,playtest,deathtest}.js | **The three CLAUDE.md gates have zero assertions** — they always exit 0; a regression where players can't die or the bell never rings still "passes" |
| P0-4 | governance | **CLAUDE.md's Never-list vs the shipped game**: six thin-veil parody celebs live on the public URL under owner-approved RISKY_CAMEOS while the constitution still forbids them, AND qa/design-cameos.md §4 still contains the un-stripped risky-tier table with right-of-publicity analysis (CLAUDE.md falsely claims it's ship-tier only). Private repo today; one visibility flip from exposure. Decide once, then make the docs say the decided thing |

---

## 1. SIMULATION CORE (15 findings)

- P0 | 1669 | 30U30 phantom farm (see P0-1).
- P0 | 1719 | Bell pre-commit payout + re-arm (see P0-2).
- P1 | 1114 | CONTRARIAN chat table (x6480) sits inside CHAD's arena — chat freezes a live fight; on exit Chad can overlap the player for a no-i-frame hit; touch stillness auto-triggers it mid-fight. (Street-level + boss-live guards were added for OPENING the chat, but the table placement itself remains inside the arena band.)
- P1 | 908 | Compliance phantom's 14×16 AABB extends 1.5–4.5px below its drawn art (visual ≥ hitbox violation) — invisible side hits.
- P1 | 1411 | Slop clone can spawn ON the player and the real one damages the same frame — untelegraphed hit.
- P2 | 1495 | Deck-killing CHAD skips MEETLY recap + ACTION ITEMS (stomp path only sets meetlyT).
- P2 | 1313 | Thought-leader THREAD cycle (%150) drifts vs its posting-glow/i-frame cycle (%140) — telegraph warns about nothing.
- P2 | 2143/2825 | Sim state ticked inside draw(): interview.respectT and barryShownT run at display rate and keep draining while paused (fixed-timestep contract breach).
- P2 | 1294 | ANGEL CHECK sets hearts=max(maxHearts,3) without raising maxHearts — SERIAL FOUNDER shows 3 hearts that a respawn silently clips to 2.
- P2 | 996 | Respawn re-arms pedApplied → CHAD's pedigree quip is unshifted AGAIN each retry, stacking duplicates in the rotation.
- P2 | 1622 | Intern coin vacuum ignores the CMO impressions rule and the NEPO +20% (pays raw COIN_VALUE, even in-frame).
- P2 | 1106 | Mini-game early-returns freeze frame-based timers (ycT/deckT/coffee/boss atkT) while playMs runs — invincibility windows can be parked mid-countdown.
- P2 | 894 | throwDeck fires during pause and inside mini-games (state stays ST.PLAY), queuing shots into the frozen sim.
- P2 | 1240 | Burn tick during i-frames prints "BURN RATE," resets burnT, then no-ops — displayed damage that did nothing.
- P2 | 606 | cofSaid written twice, read nowhere (dead state).

Sim verdict: physics + death paths genuinely solid; economy edges and content-vs-arena collisions are the debt.

## 2. RENDER PIPELINE + CHROME (12 findings)

- P1 | 3143 | lateDraws (boss quips + Waltman's sign) flushes INSIDE the hurt-flash visibility gate — quips strobe at 10Hz exactly during boss fights.
- P1 | 2760/2825/2143 | Same draw-side state mutation family as sim finding (dialogue rotation broken at 120Hz/30Hz; ticks during pause).
- P1 | 3247 | BOARD SYNC HUD row shares y54 with the PITCHING row's overflow slot — both print at (12,54) into mush when stacked (NEPO + deck + coffee).
- P2 | 2422 | Hurt-shake translates only cx: overlay text + occlude punches desync 2px for 2 frames.
- P2 | 130 | occlude() uses unrounded coords vs px()'s rounding — up to 0.5 game-px sliver misalignment at fractional TS.
- P2 | 147 | tv.height rounds independently of the CSS height floor — 1 device-px vertical text drift at fractional scales.
- P2 | 3201/3181 | MEETLY + CMO overlays occlude the TEXT but the white bubble boxes painted on cx remain — ghost empty bubbles behind the dim.
- P2 | 2674 | IS CODE DEAD subtitle (~148px) overflows its 104px backdrop by ~45px.
- P2 | 70 | #credit and #lbChip overlap on the title below ~460px viewport width.
- P2 | 58 | Portrait mode: fit() relocates tL/tR/tJ but never #tP — the ⏸ sits on the in-game timer box.
- P2 | 2501 | Posthumous CHAD.AI sign is the one backing box with no occlude().
- P2 | 2231 | Bubble stacking gives up after 8 raises and overlaps anyway (no fallback) in max-talker clusters.

## 3. FEATURES LAYER + COPY (18 findings)

- P0(governance) | 2791+ | Thin-veil celebs live vs the checked-in Never-list — needs the CLAUDE.md reconciliation (see P0-4).
- P1 | 3381 | Flair "built a real team (allegedly)" is UNREACHABLE: needs 3 hires, only 2 booths exist, each hires once.
- P1 | 741/790 | Both mini-games are beaten blind by alternating arrows: right answer is always index 0, side-swap is pure parity — L,R,L,R,L,R,L aces the interview every run.
- P1 | 703-718 | The interview's own stated meta ("the short answer is always right") is FALSE in the shipped bank — in 5 of 7 questions the wrong answer is shorter or equal.
- P1 | 3637 + api:51 | Pedigree multiplier applies to LOSSES and the board ranks raw val: an ex-SYNERGY.AI death (raised×50) outranks every pedigree-less win. A menu pick beats playing well.
- P1 | 612-619 | "Bigger multiplier = bigger baggage" is inverted: ×20+ tiers get the freebies (chat/interview +1, Chad −1HP) — the biggest multipliers make the game EASIER; TEAL FELLOW (×20) is strictly dominated (harshest baggage, smallest boosted mult).
- P1 | 684 | CONTRARIAN table in Chad's arena (same as sim P1).
- P1 | 461-483 | 16 real startup wordmarks ship at partner:false, contradicting the in-file "pending a yes" policy comment; only CLEAN is confirmed (and its evidence cell is empty).
- P2 | 3485 | 'rug token' obituary headline unreachable (rug pickup never kills).
- P2 | 563 | DOWN ROUND market copy promises "(still spawn board members)" — that mechanic doesn't exist (v1.3 removed bodies; the term-sheet board-member spec was never built).
- P2 | 1079/977/3455/3569 | ONE quoteRoll gates the mom round AND picks booth intern AND all badge/obit quotes — mom-round runs always get GRINDER + the same quote family (correlated content).
- P2 | 600 | AI cofounder is strictly worse than NONE (pure downside); BUSINESS strictly dominates TECHNICAL (~$450K vs a walk-speed nudge).
- P2 | 950 | ZERO CHURN double standard: 'conf' seed pre-kills half the meetings (flair credits ghosts) while 30U30/unpaid-intern phantoms BLOCK the flair.
- P2 | 2083 | Chat intro lines display for 0.5s — unreadable copy.
- P2 | 2143/2757 | respectT/cofSaid draw-tick family (see above) — BUSINESS/AI lines may never rotate on 30Hz.
- P2 | 3423 | "BOSSES x/3" on the win badge can only ever read 3/3 (walls force it) — fake-dynamic stat.
- P2 | 3380/3453/969 | SELF-MADE (unfundable) stacks with SEED LED BY: MOM; momBacked is set merely by PICKING mom, so the badge claims a seed round that never fired.
- P2 | 715 | "google. they don't know yet." — a real trademark in player copy; every sibling is veiled (canon rule 3 gap the grep list doesn't cover).

## 4. TEST INFRASTRUCTURE (15 findings)

- P0 | smoketest/playtest/deathtest | Zero assertions, always exit 0 (see P0-3).
- P0 | — | Cofounders, pedigree (tiers/baggage/gear/valuation), mom round: no committed probe (the /tmp probes died with the session). momRound's >20s guard protects most probes, but any die-on-purpose probe past 20s inherits a 1/6 flake.
- P0 | — | Interview + BATCH F26 + all three coffee chats: no committed probe; a freeze bug in either stops the whole world and every committed test stays green.
- P0 | — | Leaderboard client + title panel structurally untestable today (all tests run file:// so LB_ON=false); lbRowsHtml injects r.name into innerHTML unescaped, trusting server sanitization — untested on both sides.
- P1 | — | Interns: no probe (the dwell-gate was designed for bot safety, then never probed).
- P1 | — | Pause: zero coverage; a playMs leak under pause would corrupt T2D3 + leaderboard times invisibly.
- P1 | — | CMO impressions + burn death: never positively asserted ('burn' is the one obituary cause never staged).
- P1 | — | Whole suite is calendar-nondeterministic (only verify-daily-seed fakes the date): market modifiers change boss HP/enemy counts/burn cadence → day-dependent pass/fail (e.g., WITNESSED wait vs fast-burn seeds).
- P1 | fullrun.js:26-45 | Bot hand-mirrors world data (SEGS/LOW_PLATS/SIGN_STOPS/COIN_DETOURS) — already drifting (420/1060 vs moved plats), saved by tolerance.
- P1 | fullrun design | Bot certifies exactly one lane: run-right-and-stomp (never pickers, mini-games, deck, booths, house, pause, taxi, backtracking).
- P1 | celebs-probe:25 | Asserts RISKY_CAMEOS === true — pins the risky state ON; the strip would hard-fail the probe.
- P2 | celebs-probe:19 | Celeb grep-gate scans a NARROWER surface than the billboard gate (omits OBIT_CAPTIONS + badgeFlairs).
- P2 | playtest:14 + 5 probes | The documented title-start flake is unmitigated in 6 of 9 entry points.
- P2 | obituary-probe:96 | "share copy carries the headline" check cannot fail (asserts length>10 on a fresh call).
- P2 | headers/PNGs | Stale probe headers (19 boards/240px vs actual 17/250 — and Cloud's real min gap is exactly 250, zero margin), design-eggs.md documents v0.1 facts, ~130 unasserted screenshot PNGs churn in git.

Top 3 probes to write next: qa/minigames-probe.js (interview+chats+cofounder+pedigree+mom), qa/leaderboard-probe.js (local http + stubbed api + name-escaping + api unit test), and converting the three gates into asserting tests with a faked date.

## 5. DOCS vs REALITY

**Promised, never built** (beyond deliberate deferrals):
- MASTER-PLAN §1.3/§1.5: BOARD MEMBER follower from term sheets (sprite exists in design/sprites.js; a live daily-seed string still advertises it) — smallest diff, biggest canon payoff.
- MASTER-PLAN §1.7 / BIBLE §4/§11: the endings pack (ACQUI-HIRE, ZOMBIE, LIFESTYLE, DROPPED-OUT via the deferred Fellowship door) + 8-badge collection meta; SERIES B MODE (NG+); FOUNDER LARPer enemy.
- MASTER-PLAN §2.3 / ROADMAP §5: the first-30-seconds pass (first gremlin 520→650, free coin row) — still unshipped as specced.
- ROADMAP §8 step 2: result URLs + /r redirect + per-run og:image — **api/og.mjs is built and deployed but referenced by NOTHING**; the share loop's strongest viral mechanic is dead code.
- ROADMAP §10 / M4 gate: funnel events (start / first_boss / badge_screen) — the traction gate is unmeasurable; only pageviews exist.
- QUESTS-PLAN Tiers 1–3: ADVISOR, RECRUITER, GPU QUEST, INTRO CHAIN, PIVOT QUEST, BOARD MEETING EVENTS (angel + cofounder shipped in altered forms, doc never updated).
- BIBLE §5 podcast-freeze projectile; §6 MATCHA typed-egg (shipped as a placed powerup instead); §9 eggs #8–#12 (mute×10, die-10 title flavor, robotaxi 5-STARS, LARPer identity crisis).
- BUILD-GUIDE step 3 (domain) + step 5 (iOS Safari + LinkedIn Post Inspector verification); LAUNCH-PLAYBOOK end-to-end (launch never executed).
- MORNING-REPORT item (f): archive ~/Downloads/founder-mode-kit — still sitting there with a nested copy + zip.

**Stale docs (now false)** — worst offenders:
- **CLAUDE.md**: "~1,300 lines" (3,869), "4 biomes" (5), "3 eggs" (~8); TODOs (a)(M2)(M3)(M5-gate) all done/dead; claims qa/design-cameos.md is ship-tier only (false); Never-list contradicts the shipped game; verification section omits fullrun + the 9 probes.
- GAME-DESIGN.md: written against v0.1 (LEVEL_W 7500, 2 bosses, "no pause", "no backend leaderboard") — nearly every table stale.
- CLIP-REPORT.md: says humor-patch/v1.2 are unmerged/undeployed (all merged, live); regression numbers pre-date two re-benchmarks.
- PARTNERS.md: deviation section still narrates the 19-board layout; CLEAN's evidence cell empty against its own written-yes rule.
- BILLBOARDS-FINAL.md "ALL 19 CONFIRMED ✅" (17 shipped, different geometry); BILLBOARDS.md parody roster superseded; old vercel host in the outreach email.
- MORNING-REPORT/BIBLE/HUMOR-PATCH/QUESTS-PLAN/DESIGN-NOTES: all describe superseded builds (details in the full agent report inside git history of this file's PR).

**Hygiene risks:**
1. qa/design-cameos.md §4 risky-tier table NOT stripped (names + right-of-publicity analysis) — must go before any public repo flip; CLAUDE.md claims it's already gone.
2. docs/HUMOR-PATCH.md + CLIP-REPORT + celebs-probe + celeb PNGs carry the celeb names + "owner briefed 3×" acknowledgment — same pre-public-push concern.
3. index.v0.1.bak.html still contains YC/WEWORK/Patagonia strings (deploy-excluded, repo-included).
4. Real-brand billboards: paper trail (PARTNERS evidence column) doesn't match what's live; big names ship real despite the "parody-safe until yes" note.
5. .gitignore misses .vercel/ and supabase/ (org/project IDs one `git add -A` from being committed).
6. In-game copy lies: DOWN ROUND board-member string (563).

## 6. SERVER / DEPLOY SURFACE (10 findings)

- P1 | leaderboard.mjs:57-99 | Board is forgeable by curl (no auth/rate-limit/proof-of-run; service key is the only writer; CORS blocks browsers only). Row creation unbounded (one per name per seed).
- P1 | og.mjs | Dead code, still deployed, unauthenticated, `Cache-Control` keyed on query string — cache-busting compute amplification on an endpoint nothing calls. Either wire it into the share loop (ROADMAP §8) or delete it.
- P2 | og.mjs:20 | Output wildly stale vs the real badge (no name/valuation/pedigree) if ever wired as-is.
- P2 | .vercelignore | Blacklist omits supabase/ and .vercel/ — currently safe only via Vercel's dotfile-skip; blacklist fragility on a must-stay-closed surface.
- P2 | .gitignore | .vercel/ + supabase/.temp/ not ignored.
- P2 | leaderboard.mjs:95 | Upsert race: concurrent first posts → unique-index 409 → client shows "board offline" though the row exists (no PATCH fallback/retry).
- P2 | leaderboard.mjs:27 | Profanity filter gaps: no 2/6/9/@ leet folding ("F@GG" slips), and substring false-positives (HANCOCK, DICKSON, SCUNTHORPE get rejected).
- P2 | index.html:7-14 | Meta tags thin: no og:url/og:type/twitter:image (fallback-reliant unfurls).
- P2 | package.json | @vercel/og caret-ranged, not pinned (lockfile mitigates only on deterministic installs).
- P2 | index.html:92 | Credit link rel="noopener" without noreferrer (minor).

## 7. CONVERSATION-PROMISED vs BUILT + PRIORITIZED RECOMMENDATIONS

*(Conversation table in section 0 above.)* Priority order, effort-weighted:

1. **Kill the two economy exploits** (P0-1, P0-2): cap/decay 30U30 phantom rewards; move the bell's +$1M inside the win commit and set bellDone (or a bellPaid flag) at meter-full. The leaderboard's integrity depends on both.
2. **Make the three gates assert** + commit qa/minigames-probe.js and qa/leaderboard-probe.js (P0-3, test P0s). One session of work; converts "always green" into an actual gate.
3. **CLAUDE.md reconciliation + cameo-doc strip** (P0-4): one honest hour — update the constitution to the owner's actual rulings (celebs: approved-with-gates), fix the numbers/TODOs, strip qa/design-cameos.md §4, note index.v0.1.bak in the public-push checklist, add .vercel/+supabase/ to .gitignore.
4. **Leaderboard integrity, design-level** (features P1): don't rank losses × pedigree above wins (rank by (won, val) or divide loss-pedigree), and decide the pedigree-freebies inversion (baggage should scale WITH mult). Server-side: add a minimal shared-secret/HMAC on POST or accept forgeability consciously for a joke board.
5. **The share loop** (docs top-value #1): wire /api/og into result URLs (or delete it and refresh og.png to current badge art) — the strongest unbuilt viral mechanic, mostly already written.
6. **Fix the felt bugs**: lateDraws outside the flash gate; BOARD SYNC HUD row collision; unreachable "built a real team" flair (add a third hiring source or lower to 2); mini-game answer-side randomization (use dailyRng per question, not parity); interview bank rewrite so the short answer IS right; chat intro display time.
7. **Funnel events** (~20 lines) before launch; then **execute LAUNCH-PLAYBOOK** — the game is more finished than its launch.
8. **Board-member-from-term-sheets** (smallest high-canon content item; a live string already promises it) + first-30-seconds pass.
9. Backlog with judgment: endings pack, domain purchase, all-time board tab, per-pedigree investor-NPC variants, QUESTS tier ideas, world-stretch for spaciousness (large; only if the game still feels cramped after play).
10. Docs sweep for everything in section 5's stale list + CHANGELOG catch-up entries (v1.2/v1.3/deep-clean/parade/finale-difficulty were never logged).
